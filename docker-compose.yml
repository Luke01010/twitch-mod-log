services:
  twitch-mod-log:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env

  yt-mod-log:
    build:
      context: .
      dockerfile: Dockerfile.yt
    restart: unless-stopped
    env_file: .env
